// JavaScript
/*document.addEventListener("DOMContentLoaded", function() {
    const replyButton = document.getElementById("reply-button");

    replyButton.addEventListener("click", function(event) {
        event.preventDefault();  // Empêche le rechargement de la page lors de la soumission du formulaire

        const destinataireEmail = document.getElementById("adresse_mail").innerText;
        const messageContenu = "Re : " + document.querySelector(".contenu_message").textContent.trim();

        document.getElementById("participant-input").value = destinataireEmail;
        document.getElementById("message-input").value = messageContenu;
    });
});*/
